/*
*
* Step 3 - Calculate Total Fare For Multiple Rides
*
Given a list of multiple Rides, the Invoice Generator should calculate the aggregate total for all.

You can assume that you are given a list of Ride objects
For e.g [Ride(d1, w1), Ride(d2, w2), Ride(d3, w3),...]

* */